# Printarr Backend Dependencies
# Install: pip install -r requirements.txt

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6  # Required for file uploads

# Database (PostgreSQL for production per DEC-039)
sqlalchemy>=2.0.25
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
greenlet>=3.0.0
# SQLite for tests only
aiosqlite>=0.19.0

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Logging
structlog>=24.1.0

# Telegram MTProto Client
telethon>=1.36.0

# HTTP Client (for external APIs like Thangs)
httpx>=0.27.0

# Archive Extraction (v0.5)
rarfile>=4.1
py7zr>=0.20.0

# Preview Image Handling (v0.7)
aiofiles>=24.1.0
Pillow>=10.0.0

# Google Drive Integration (v0.8)
# Note: google-auth-oauthlib 1.2.x requires google-auth<2.42.0
google-api-python-client>=2.100.0
google-auth>=2.22.0,<2.42.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# Encryption (for storing OAuth tokens)
cryptography>=41.0.0

# File System Monitoring (v0.8 Bulk Import)
watchdog>=3.0.0

# Fuzzy String Matching (v0.9 Deduplication)
rapidfuzz>=3.5.0
